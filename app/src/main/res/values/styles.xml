<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="ViewerTheme" parent="Theme.AppCompat.NoActionBar"></style>
    <style name="AppCompatTheme" parent="MaterialDrawerTheme.TranslucentStatus">
    </style>
</resources>
